# Blood Sugar Monitoring System - Environment Configuration
# =========================================================
# Copy this file to .env and fill in your actual values
# DO NOT commit the .env file to version control (it's in .gitignore)

# ===== EMAIL CONFIGURATION (SMTP) =====
# For Gmail: 
#   1. Enable 2-Step Verification in Google Account settings
#   2. Go to: Google Account > Security > 2-Step Verification > App Passwords
#   3. Generate an App Password for "Mail" on "Windows Computer"
#   4. Use the 16-character password (no spaces) as SMTP_PASSWORD
#
# For other email providers:
#   - Outlook: smtp-mail.outlook.com (port 587)
#   - Yahoo: smtp.mail.yahoo.com (port 587)
#   - Check your provider's SMTP settings documentation

SMTP_SERVER=smtp.gmail.com
SMTP_PORT=587
SMTP_USERNAME=your-email@gmail.com
SMTP_PASSWORD=your-app-password-here

# ===== DATABASE CONFIGURATION =====
# MySQL/MariaDB database connection settings
DB_HOST=localhost
DB_PORT=3306
DB_USER=root
DB_PASSWORD=your-database-password
DB_NAME=blood_sugar_db

# ===== APPLICATION CONFIGURATION =====
# Secret key for JWT token generation (change this to a random string)
JWT_SECRET_KEY=your-secret-key-here-change-this

# Flask debug mode (set to False in production)
FLASK_DEBUG=True

# ===== NOTIFICATION SETTINGS =====
# How many abnormal readings trigger an alert
ALERT_THRESHOLD=3

# How many days to look back for abnormal readings
ALERT_WINDOW_DAYS=7
